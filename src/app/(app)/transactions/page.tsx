import { ComingSoon } from "@/components/coming-soon";
export const metadata = { title: "Transactions · Finance OS" };
export default function Page() {
  return (
    <ComingSoon
      title="Transactions"
      description="Income and expense entry, categories, tags, recurring items and CSV import land here in Phase 1."
    />
  );
}
