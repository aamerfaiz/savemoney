import { ComingSoon } from "@/components/coming-soon";
export const metadata = { title: "Settings · Finance OS" };
export default function Page() {
  return (
    <ComingSoon
      title="Settings"
      phase="Phase 1"
      description="Profile, base currency, categories and account preferences."
    />
  );
}
