import { ComingSoon } from "@/components/coming-soon";
export const metadata = { title: "Goals · Finance OS" };
export default function Page() {
  return (
    <ComingSoon
      title="Savings Goals"
      description="Set targets and deadlines, track contributions and see expected completion dates."
    />
  );
}
